class NotesState{
  List<Map<String,dynamic>> allData;
  NotesState({required this.allData});
}

class editViewState{
  List<Map<String,dynamic>> editData;
  editViewState({required this.editData});
}